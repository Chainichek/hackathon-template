spring:
  application:
    name: auth
  jpa:
    hibernate:
      ddl-auto: none
  liquibase:
    change-log: classpath:db/changelog/changelog.xml

security:
  token:
    secret: secret
    expire: 10000000